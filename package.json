{
  "name": "gmail-forward-webapp",
  "version": "2.0.0",
  "description": "Hosted Gmail forwarding automation — confirmation email sent to recipient, login happens on their own device",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install chromium"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.18.0",
    "playwright": "^1.47.0",
    "nodemailer": "^6.9.14"
  },
  "engines": { "node": ">=18.0.0" }
}
