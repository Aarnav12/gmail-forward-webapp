{
  "name": "gmail-forward-smooth",
  "version": "1.0.0",
  "description": "Link-based Gmail forwarding automation with live in-browser streaming — no email, no downloads, no terminal for the second user",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "PLAYWRIGHT_BROWSERS_PATH=0 npx playwright install chromium"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.18.0",
    "playwright": "^1.47.0"
  },
  "engines": { "node": ">=18.0.0" }
}
